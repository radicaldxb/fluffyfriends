# Required for Next.js routes (e.g. /create) and API routes to work on Netlify
[[plugins]]
  package = "@netlify/plugin-nextjs"

[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  # Environment variables available at build and runtime
  NEXT_PUBLIC_SUPABASE_URL = "https://mblnpneghvkfmbgmbrco.supabase.co"
  NEXT_PUBLIC_SUPABASE_ANON_KEY = "sb_publishable_eigN9VshsqrTV5jXD4nFjw_9CKI6pnQ"
  # Use PRODUCTION URL so the workflow picks up triggers when Active (not webhook-test).
N8N_WEBHOOK_URL = "https://n8n.srv943460.hstgr.cloud/webhook/transform-pet"

# Optional: N8N_PING_WEBHOOK_URL = your test-production-url (for "Ping webhook" on /test-n8n).
# If set, Ping uses it; /create always uses N8N_WEBHOOK_URL. Add in Netlify UI.
#
# IMPORTANT: Also add these in Netlify UI for reliability:
# Site settings → Build & deploy → Environment variables
# UI vars override netlify.toml and are more reliable
