[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  # Environment variables available at build and runtime
  NEXT_PUBLIC_SUPABASE_URL = "https://mblnpneghvkfmbgmbrco.supabase.co"
  NEXT_PUBLIC_SUPABASE_ANON_KEY = "sb_publishable_eigN9VshsqrTV5jXD4nFjw_9CKI6pnQ"
  N8N_WEBHOOK_URL = "https://n8n.srv943460.hstgr.cloud/webhook-test/transform-pet"

# IMPORTANT: Also add these in Netlify UI for reliability:
# Site settings → Build & deploy → Environment variables
# UI vars override netlify.toml and are more reliable
